apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  labels:
        app: new
 # namespace: new
spec:
  #type: NodePort
  selector:
    app: new
  ports:
    - port: 80
      targetPort: 80
     # nodePort: 30008
